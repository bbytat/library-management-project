#ifndef PROTOTYPES_H
#define PROTOTYPES_H

void get(char *pt);
void viderBuffer();

    // MEMBER PROTOTYPE :
void show(struct Member);
void ask(member *ptr);

    //BOOK PROTOTYPE :
void show(struct Book);
void ask(book, *ptr);

#endif